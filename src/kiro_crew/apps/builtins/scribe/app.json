{
  "name": "scribe",
  "version": "0.1.0",
  "displayName": "Scribe",
  "description": "Write markdown documents beside an agent co-author. The co-author panel is a real Kiro Crew chat session scoped to the document, so the agent brings your memory, knowledge library, and codebase context to the draft — and can edit the document while you watch it update.",
  "author": "pevu",
  "license": "Apache-2.0",
  "tags": [
    "markdown",
    "writing",
    "documents",
    "co-author"
  ],
  "highlights": [
    "Markdown editor with the document rendered beside the source",
    "AI co-author chat scoped to the document — a full Kiro Crew session with your memory and tools",
    "The agent edits the document on disk; the editor reloads when its turn finishes",
    "Documents are plain .md files — portable, diffable, readable by any editor"
  ],
  "defaultEnabled": false,
  "permissions": {
    "api": [
      "/api/apps/scribe",
      "/api/apps/scribe/*",
      "/api/chat",
      "/api/chat/*"
    ],
    "storage": true
  },
  "backend": {
    "routes": "backend.routes:register_routes"
  },
  "ui": {
    "pages": [
      {
        "route": "/scribe",
        "label": "Scribe",
        "icon": "PenLine"
      }
    ]
  },
  "platform": {
    "os": [
      "macos",
      "linux",
      "windows"
    ]
  },
  "iconUrl": "/app-assets/scribe/icon.svg",
  "screenshots": [
    "/app-assets/scribe/screenshot-main.png"
  ],
  "useCases": [
    "Draft a design doc or notes in markdown while a document-scoped agent supplies implementation details from your codebase and research from your memory."
  ],
  "configuration": [
    "Enable the app, create a document, and start a co-author session from the panel — documents are stored as plain .md files in the app's data folder."
  ]
}
